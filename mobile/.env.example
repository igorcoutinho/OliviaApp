# Não é mais necessário um .env na raiz.
# Ambientes ficam em:
#   envs/local.env  → APP_ENV=local (npm run start:local)
#   envs/prd.env    → APP_ENV=prd   (npm run start:prd)
#
# O app.config.js lê o arquivo conforme APP_ENV e injeta apiUrl/appSecret.

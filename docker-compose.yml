services:
  postgres:
    image: postgres:16-alpine
    container_name: festa-olivia-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: olivia
      POSTGRES_PASSWORD: olivia123
      POSTGRES_DB: festa_olivia
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U olivia -d festa_olivia"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: festa-olivia-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin123
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - minio_data:/data

  minio-init:
    image: minio/mc:latest
    container_name: festa-olivia-minio-init
    depends_on:
      minio:
        condition: service_started
    entrypoint: >
      /bin/sh -c "
      mc alias set local http://minio:9000 minioadmin minioadmin123;
      mc mb local/festa-olivia --ignore-existing;
      mc anonymous set download local/festa-olivia;
      exit 0;
      "

volumes:
  postgres_data:
  minio_data:
